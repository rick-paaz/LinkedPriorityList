package model;

import java.util.Iterator;

/**
 * @author YOUR NAME
 *
 * @param <T> The type of elements to store in this PriorityList
 */

// PriorityList objects store a collection of elements as an indexed list.
public class LinkedPriorityList<T> implements PriorityList<T>, Iterable<T> {
  
  // Suggestion: 
  // Copy and Paste the method headings from PriorityList.java.
  // Change the method headings ending with ; into method stubs.
  // This way you get to keep the Javadoc comments in interface PriorityList.
  // You also get rid of the error on the line above.

}
